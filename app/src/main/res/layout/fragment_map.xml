<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- MapView -->
    <com.mapbox.maps.MapView
        android:id="@+id/mapView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:mapbox_locationComponentEnabled="true"
        app:mapbox_locationComponentPuckBearing="heading">

        <!-- Layout chứa các phần tử UI -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="75dp"
            android:orientation="horizontal"
            android:background="@drawable/border_background_map"
            android:padding="8dp"
            android:gravity="center">

            <LinearLayout
                android:layout_width="222dp"
                android:layout_height="match_parent"
                android:orientation="horizontal">

                <ImageView
                    android:id="@+id/avatarImageView"
                    android:layout_width="50dp"
                    android:layout_height="50dp"
                    android:layout_marginEnd="16dp"
                    android:background="@drawable/border_background_map"
                    android:scaleType="centerCrop"
                    android:src="@drawable/avartar" />

                <!-- Popup TextView -->
                <TextView
                    android:id="@+id/popupTextView"
                    android:layout_width="151dp"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="16dp"
                    android:background="@drawable/border_background"
                    android:elevation="4dp"
                    android:padding="8dp"
                    android:text="Không Hoạt Động"
                    android:textColor="@android:color/black"
                    android:visibility="visible"
                    android:textAlignment="center"/>

            </LinearLayout>

            <!-- Button với icon -->
            <ImageButton
                android:id="@+id/imageButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_shutdow_dark"
                android:layout_gravity="center"
                android:scaleType="centerInside"
                android:adjustViewBounds="true"
                android:background="@drawable/rounded_background"
                />

        </LinearLayout>



    </com.mapbox.maps.MapView>

    <!-- Popup layout ẩn ban đầu -->

</FrameLayout>
